// critical: convert the time to minutes first so we can compare times
// 1 hour has 60 minutes, multiply the exam/arrive hours by 60 we get the minutes, add it to the exam/arrive time minutes
//var totalMinutes = parseInt(mins) + (parseInt(hours) * 60);

    //convert times to minutes
    // calculate 30 minute arrival window time
    // variables to calc mins early/late

    // if/else if statements
    // first - print arrival time late/early/on time
    // next - calc mins early/late to carry forward

    // nested if else if
    // if early - print how early (and not exactly on time) by converting mins back to hours
    //  add leading zero
    // hoursEarly = Math.floor(minsEarly / 60);//this gives you the hours
    // minsEarly = minsEarly - (hoursEarly * 60);//this gives you the mins

    // nested if else if
    // if late - print how late (and not exactly on time) by converting mins back to hours
    //  add leading zero
    // hoursLate = Math.floor(minsLate / 60);//this gives you the hours
    // minsLate = minsLate - (hoursLate * 60);//this gives you the minutes